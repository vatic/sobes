import { Monoid } from 'fp-ts/lib/Monoid'

// concat is (<>) in Haskell
// export interface Semigroup<A> {
//   readonly concat: (x: A, y: A) => A
// }

// const onlyOne: Semigroup<number> = {
//   concat: (x, y) => 1,
// }

// export interface Monoid<A> extends Semigroup<A> {
//   readonly empty: A
// }

